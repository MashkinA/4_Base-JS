// Строки
// Создание строки

const name = 'Alexander';
console.log(name);

// '' or ""

const surname = "Mashkin";
console.log(surname);

// Объединение строк (конкатенация)

console.log(name+surname);

// Шаблоные строки

const fullname = `Name Family: ${name} ${surname}`;
console.log(fullname);

// Длина строки

console.log(name.length);